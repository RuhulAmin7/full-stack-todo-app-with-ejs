<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@5.2.0/dist/css/bootstrap.min.css">
    <title>Todo</title>

</head>

<body>
    <header class="bg-success py-2 text-end">
        <div class="container">
            <a href="/logout">
                <button class="btn btn-warning">Logout</button>
            </a>
        </div>
    </header>
    <div class=" container card p-4 my-4 " style=" max-width: 800px;">
        <h4 class="text-center text-light rounded p-1 mb-4 bg-danger">Add a new task</h4>
        <form action="/addnewtask" method="post">
            <div class=" d-flex gap-2 align-items-center">
                <input type="text" name="title" placeholder="Task Title" class="form-control" id="title">
                <select class="form-control" id="status" name="priority">
                    <option disabled selected>--Select Priority--</option>
                    <option value="high">High</option>
                    <option value="medium">Medium</option>
                    <option value="low">Low</option>
                </select>
                <input type="date" name="date" id="date" class="form-control" id="date">
                <button type="submit" class="btn btn-success">Add</button>
            </div>
        </form>
    </div>

    <div class="display-task container mt-5  pt-3">
        <div>
            <h4 class="text-center bg-secondary rounded p-1 text-light">Your task list</h4>
            <table class="table table-striped table-bordered table-hover">
                <% if(todos.length>0) {%>

                    <thead>
                        <tr>
                            <th>SL</th>
                            <th>Title</th>
                            <th>Priority</th>
                            <th>Status</th>
                            <th>Date</th>
                            <th>Action</th>
                        </tr>
                    </thead>
                    <tbody>
                        <% todos.map((todo, index)=> { %>
                            <tr>
                                <td>
                                    <%= index +1 %>
                                </td>
                                <td>
                                    <%= todo.title %>
                                </td>
                                <td>
                                    <%= todo.priority %>
                                </td>
                                <td>
                                    <%= todo.status %>
                                </td>
                                <td>
                                    <%= todo.date.toDateString() %>
                                </td>
                                <td>
                                    <button date-id="<%=todo._id%>" class="btn btn-warning">Edit</button>
                                    <a href="/deletetask/<%=todo._id%>" class="btn btn-danger">Delete</a>
                                </td>
                            </tr>
                            <% }) %>

                    </tbody>
                    <% }else{ %>
                        <h4 class="text-warning text-center p-4">You didn't add any tasks</h4>
                        <% } %>

            </table>
        </div>
    </div>
</body>

</html>